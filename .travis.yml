sudo: required
services:
    - docker


before_install:
    - docker build -t huntercongress/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true huntercongress/docker-react npm run test